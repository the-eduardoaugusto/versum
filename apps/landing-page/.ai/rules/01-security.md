# Security Rules (Landing Page)

## Static Content
- Never expose secrets or credentials in client-side code
- Use environment variables for any configuration

## Dependencies
- Keep dependencies updated to patch security vulnerabilities
- Audit dependencies for known vulnerabilities regularly
