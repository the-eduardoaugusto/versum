# Client App Agent Context

This is the Next.js client application.

## Stack
- Next.js (App Router)
- TypeScript
- Tailwind CSS (or your current CSS solution)

## Important Conventions
- Read `.ai/docs/` for naming conventions and API patterns
- Read `.ai/rules/` for development best practices
- Follow the existing code style in the project

## Before Making Changes
1. Read all files in `apps/client/.ai/` for context
2. Check existing components to understand patterns
3. Use the global `.ai/docs/` if specific guidance not found here
