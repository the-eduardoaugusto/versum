import type { Metadata } from "next";
import { LoginScreen } from "@/features/login";

export const metadata: Metadata = {
  title: "Entrar",
  description: "Faça login com seu email",
};

export default LoginScreen;
