# API App Agent Context

This is the backend API application.

## Stack
- Node.js with your chosen framework (check package.json)
- Database: (check your current setup)

## Important Conventions
- Read `.ai/docs/` for naming conventions and API patterns
- Read `.ai/rules/` for development best practices
- Follow the global `.ai/docs/` conventions for database and API design

## Before Making Changes
1. Read all files in `apps/api/.ai/` for context
2. Check existing routes/controllers to understand patterns
3. Use the global `.ai/docs/` if specific guidance not found here
